import 'package:flutter/material.dart';

class AirplayPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AirPlay Page'),
      ),
      body: Center(
        child: Text('AirPlay Page'),
      ),
    );
  }
}
